import { EntityResolverDirective } from './entity-resolver.directive';

describe('EntityResolverDirective', () => {
  it('should create an instance', () => {
    const directive = new EntityResolverDirective();
    expect(directive).toBeTruthy();
  });
});
