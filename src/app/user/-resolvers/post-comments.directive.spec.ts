import { PostCommentsDirective } from './post-comments.directive';

describe('PostCommentsDirective', () => {
  it('should create an instance', () => {
    const directive = new PostCommentsDirective();
    expect(directive).toBeTruthy();
  });
});
