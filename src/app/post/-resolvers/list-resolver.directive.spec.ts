import { ListResolverDirective } from './list-resolver.directive';

describe('ListResolverDirective', () => {
  it('should create an instance', () => {
    const directive = new ListResolverDirective();
    expect(directive).toBeTruthy();
  });
});
